// Model for the party data fields
class Party {
  final String title;
  final DateTime date;
  final String location;
  final int population;
  final String description;

  Party(this.title, this.date, this.location, this.population, this.description);
}